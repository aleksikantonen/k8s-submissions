[project]
name = "todo-app"
version = "0.1.0"
description = "Simple todo web server for Kubernetes"
requires-python = ">=3.11"
dependencies = [
    "flask>=2.3",
    "gunicorn>=21.2",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
